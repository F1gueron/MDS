Esta vez la agenda está implementada en Python, nos da las mismas opciones que anteriormente. Dependiendo de la versión de Python, `input()` (mirar versión) funcionaba internamente con `eval()` lo que daba lugar a que si introducías el nombre de una variable, podría ser que se imprimiese el contenido de la variable cuyo nombre has introducido. En el Dockerfile hay un comentario acerca de la versión de Python (mirar comentario bien para poder ponerlo en el word) Podemos ver que si lo ejecutamos en local y creamos una nota escribiendo `flag`, al printearla con la segunda opción nos devolverá la palabra flag, que es lo que hemos introducido. Pero si probamos en remoto, nos devolverá el string definido en el código. Comprobando el docker-compose podemos ver que la flag se encuentra en la variable de entorno `CHALLENGE_FLAG`, y como la librería `os` está importada, podremos crear una nota con texto `os.getenv("CHALLENGE_FLAG")` y al elegir la segunda opción para leer su contenido, nos devolverá la flag -> URJC{0ld_Pyth0n_1s_b3st_pyth0n}
